# Core
python-dotenv>=1.0.0
aiohttp>=3.9.0
flask>=3.0.0

# EVM / Web3
web3>=6.0.0
eth-account>=0.10.0
eth-abi>=4.0.0

# Platform SDKs
hyperliquid-python-sdk>=0.8.0
# lighter-sdk  # install from GitHub if needed
ostium-python-sdk>=0.1.0

# Solana (for Drift) — optional, install manually if needed
# solders>=0.20.0
# solana>=0.30.0
# anchorpy>=0.19.0
# driftpy>=0.7.0
base58>=2.1.0

# Utilities
requests>=2.31.0
